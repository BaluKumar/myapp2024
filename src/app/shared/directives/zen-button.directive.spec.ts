import { ZenButtonDirective } from './zen-button.directive';

describe('ZenButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ZenButtonDirective();
    expect(directive).toBeTruthy();
  });
});
